# APP
PORT=3006
NODE_ENV=development
VERSION=1.0.0

#DB
DB_TYPE=postgres
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=postgres
DB_PORT=5432
DB_NAME=fastcar
DB_SCHEMA=fastcar
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=${DB_SCHEMA}

# JWT
JWT_SECRET=secretKeyJwt

# ADAPTERS
ORM_ADAPTER=prisma # prisma | memory | typeorm

# MAILER
MAILER_USER=maieler_user
MAILER_PASS=mailer_pass
